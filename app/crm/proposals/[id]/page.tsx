import{cookies}from"next/headers";import{notFound,redirect}from"next/navigation";import Link from"next/link";import{CRM_COOKIE,verifyCrmSession}from"@/lib/crm-auth";import{getProposal}from"@/lib/proposals";import{getLead}from"@/lib/acquisition/leads";import ProposalEditor from"./ProposalEditor";
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){if(!verifyCrmSession((await cookies()).get(CRM_COOKIE)?.value))redirect("/crm/login");const p=await getProposal((await params).id);if(!p)notFound();const lead=await getLead(p.leadId);if(!lead)notFound();return <main className="section simple-page crm-page"><Link href={`/crm/leads/${lead.id}`}>← {lead.businessName}</Link><p className="kicker kicker-gold">Proposal v{p.version}</p><h1>{p.title}</h1><ProposalEditor initial={p} clientName={lead.businessName}/></main>}
