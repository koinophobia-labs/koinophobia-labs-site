import{cookies}from"next/headers";import{notFound,redirect}from"next/navigation";import Link from"next/link";import{CRM_COOKIE,verifyCrmSession}from"@/lib/crm-auth";import{getAudit}from"@/lib/audits";import{getLead}from"@/lib/acquisition/leads";import AuditEditor from"./AuditEditor";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){if(!verifyCrmSession((await cookies()).get(CRM_COOKIE)?.value))redirect("/crm/login");const audit=await getAudit((await params).id);if(!audit)notFound();const lead=await getLead(audit.leadId);if(!lead)notFound();return <main className="section simple-page crm-page"><Link href={`/crm/leads/${lead.id}`}>← {lead.businessName}</Link><p className="kicker kicker-gold">Measured website audit</p><h1>{audit.finalUrl||audit.targetUrl}</h1><AuditEditor initial={audit}/></main>}
