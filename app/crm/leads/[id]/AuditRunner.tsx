"use client";import{useState}from"react";import{useRouter}from"next/navigation";export default function AuditRunner({leadId,defaultUrl}:{leadId:string;defaultUrl:string}){const[url,setUrl]=useState(defaultUrl),[message,setMessage]=useState(""),router=useRouter();async function run(){setMessage("Running measured checks. This can take up to a minute…");const r=await fetch(`/api/crm/leads/${leadId}/audits`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({targetUrl:url})}),j=await r.json();if(r.ok)router.push(`/crm/audits/${j.audit.id}`);else setMessage(j.error||"Audit failed")};return <div className="audit-runner"><input aria-label="Website URL" value={url} onChange={e=>setUrl(e.target.value)}/><button className="btn btn-cyan" onClick={run}>Run website audit</button><p role="status">{message}</p></div>}
