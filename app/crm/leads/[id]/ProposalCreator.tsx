"use client";import{useState}from"react";import{useRouter}from"next/navigation";
export default function ProposalCreator({leadId}:{leadId:string}){const[template,setTemplate]=useState("website"),[busy,setBusy]=useState(false),router=useRouter();async function create(){setBusy(true);const r=await fetch(`/api/crm/leads/${leadId}/proposals`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({template})}),j=await r.json();if(r.ok)router.push(`/crm/proposals/${j.proposal.id}`);else{alert("Could not create proposal.");setBusy(false)}}return <div className="proposal-create"><select aria-label="Service template" value={template} onChange={e=>setTemplate(e.target.value)}><option value="website">Website / landing page</option><option value="redesign">Website redesign</option><option value="automation">AI workflow / automation</option><option value="mvp">MVP prototype</option><option value="custom">Custom project</option></select><button className="btn btn-cyan" onClick={create} disabled={busy}>{busy?"Creating…":"Create proposal"}</button></div>}
