export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="section simple-page"><p className="kicker kicker-gold">Private workspace</p><h1>CRM sign in</h1><form className="panel crm-login" action="/api/crm/login" method="post"><label>Admin secret<input name="secret" type="password" required autoComplete="current-password"/></label>{p.error&&<p role="alert">That secret was not accepted.</p>}<button className="btn btn-cyan" type="submit">Sign in</button></form></main>}
