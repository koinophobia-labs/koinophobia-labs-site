import type { Metadata } from "next";
import { CTABand, ProcessStep, SectionIntro } from "@/components/studio";
import StudioFooter from "@/components/studio/StudioFooter";
import StudioNav from "@/components/studio/StudioNav";
import { processSteps, studioConfig } from "@/lib/commercial";

export const metadata: Metadata = { title: "Delivery Process", description: "How Koinophobia Labs scopes, builds, reviews, launches, supports, and hands off small-business websites and workflows.", alternates: { canonical: "/process" }, openGraph: { url: "/process" } };
export default function ProcessPage() { return <div className="studio-site"><StudioNav /><main><header className="studio-page-hero"><div className="studio-container studio-page-hero__grid"><div><p className="studio-eyebrow">Delivery process</p><h1>Clear decisions before code.</h1><p className="studio-page-hero__lede">Working with a solo studio should feel direct, not informal. Scope, responsibilities, review rounds, ownership, timeline, and support are written down before the build begins.</p></div><aside className="studio-page-hero__aside">Timelines begin after scope approval, required content and access are received, and any applicable deposit or initial payment is complete.</aside></div></header><section className="studio-section studio-section--compact"><div className="studio-container"><SectionIntro eyebrow="From diagnosis to support" title="Six visible delivery steps" body="Each step produces a decision or artifact the client can verify before the next one begins." /><div className="studio-process-grid">{processSteps.map((step) => <ProcessStep {...step} key={step.number} />)}</div></div></section><section className="studio-section studio-section--panel"><div className="studio-container"><SectionIntro eyebrow="Reliability details" title="What gets documented" /><div className="studio-policy-grid"><article className="studio-policy-card"><h3>Communication</h3><p>Clients work directly with Blake. The update cadence is agreed during scoping, important decisions are documented, and no account-manager handoff is introduced.</p></article><article className="studio-policy-card"><h3>Revisions</h3><p>{studioConfig.revisionCopy}</p></article><article className="studio-policy-card"><h3>Post-launch support</h3><p>{studioConfig.postLaunchSupportCopy}</p></article><article className="studio-policy-card"><h3>Ownership</h3><p>{studioConfig.ownershipCopy}</p></article><article className="studio-policy-card"><h3>Third-party costs</h3><p>Required subscriptions, payment fees, hosted services, and ongoing operational dependencies are disclosed before implementation.</p></article><article className="studio-policy-card"><h3>Timeline dependencies</h3><p>The schedule begins after approval, content, access, and applicable initial payment are ready. Client delays may require a revised delivery date.</p></article></div></div></section><CTABand /></main><StudioFooter /></div>; }
